#include <bits/stdc++.h>
using namespace std;
#define FASTIO                         \
	ios_base::sync_with_stdio(false);  \
	cin.tie(nullptr);                  \
	cout.tie(nullptr); 
#define el '\n'

int sum(int arr[], int size)
{
    if (!size) return arr[index];
}

int main() {
    int 
}