#include <bits/stdc++.h>
using namespace std;

// added the two lines below
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);

  int n, k, t;
  cin >> n;
  int cnt = 0;
  for (int i = 0; i < n; i++) {
    cin >> t;
    cout << t;
  }
  cout << cnt << "\n";
  return 0;
}
